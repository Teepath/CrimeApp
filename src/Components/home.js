import React from 'react';
import '../firebaseDb';


const Home = (props) =>{

    
    return(

        <div>
            <button onClick= {props.login}>Signing</button>

            </div>
    )
}

export default Home